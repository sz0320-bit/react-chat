import {BiLogOut} from "react-icons/all";
import firebase from "../firebase";
const MainHead = ({show}) => {
    return (

        <div className={`flex w-full h-20 justify-center font-mono  text-4xl items-center absolute top-3 rounded-3xl w-[98%] primary `}>
            REACT CHAT APP
        </div>

    )
}

export { MainHead };